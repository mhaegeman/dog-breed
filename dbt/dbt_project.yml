name: dog_breed_explorer
version: "1.0.0"
config-version: 2

profile: dog_breed_explorer

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]
analysis-paths: ["analyses"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  dog_breed_explorer:
    staging:
      +materialized: view
      +schema: staging
    marts:
      +materialized: table
      +schema: marts
